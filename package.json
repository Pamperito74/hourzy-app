{
  "name": "hourzy-app",
  "version": "1.0.0",
  "description": "Private, fast time tracking. No account. No cloud. Your data stays on your device.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "test:integration": "node scripts/run-browser-integration.mjs"
  },
  "dependencies": {
    "papaparse": "^5.4.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "@playwright/test": "^1.49.0"
  }
}
